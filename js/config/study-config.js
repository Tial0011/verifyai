/**
 * VERIFY-AI — Study configuration shared across participant pages.
 *
 * Kept in one place, as configuration rather than hard-coded in markup,
 * because these numbers are working values from the current protocol and
 * may change after pilot/researcher decisions (see phase-2 brief, §12).
 */

/** Total number of clinical cases in the study. Confirm before locking. */
export const TOTAL_CASES = 8;

/** Which case this template currently represents. */
export const CURRENT_CASE_NUMBER = 1;

/** Participant-facing study name shown in the status bar. */
export const STUDY_NAME = "Clinical Decision-Making Study";
